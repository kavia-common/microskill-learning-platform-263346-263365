# Backend environment variables (copy to .env for local dev)
PORT=3001
FRONTEND_ORIGIN=http://localhost:3000

# Optional: Supabase persistence (used only if provided)
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
# SUPABASE_ANON_KEY=
